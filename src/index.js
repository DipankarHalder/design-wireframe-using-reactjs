import React from 'react';
import ReactDOM from 'react-dom';
import Router from './components/Router';
import './style/style.scss';

const renderElement = document.getElementById('root');
ReactDOM.render(<Router />, renderElement);
